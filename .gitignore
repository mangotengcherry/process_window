# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtualenvs / IDE
.venv/
venv/
.env
.idea/
.vscode/

# Synthetic sample data (regenerate with: python -m src.app --cli --generate-sample)
data/sample_l1.csv
data/sample_l3.csv
data/current_specs.csv

# outputs/* are runtime artifacts. The HTML report + small CSVs are kept as a
# showcase so first-time visitors can see what the prototype produces without
# running it. Everything is regenerable.
outputs/*.tmp
