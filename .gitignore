# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtualenvs / IDE
.venv/
venv/
.env
.idea/
.vscode/

# Sample data + outputs are committed as a showcase so first-time visitors can
# clone & open outputs/evaluation_report.html and view data/sample_*.csv without
# running anything. Regenerate any time via:
#   python -m src.app --cli --generate-sample --train --recommend --report
outputs/*.tmp
